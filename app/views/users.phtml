<section class="users-header item-header">
    <header>
        <h2>Utilisateurs</h2>
        <p>Liste des utilisateurs et indicateurs clés</p>
    </header>
</section>

<?php if (!empty($_SESSION['error_message'])): ?>
<div class="alert alert-error"><?= htmlspecialchars($_SESSION['error_message']); unset($_SESSION['error_message']); ?></div>
<?php endif; ?>
<?php if (!empty($_SESSION['success_message'])): ?>
<div class="alert alert-success"><?= htmlspecialchars($_SESSION['success_message']); unset($_SESSION['success_message']); ?></div>
<?php endif; ?>

<section class="users-stats salads-stats">
    <div class="stats-grid">
        <article class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-info">
                <h3>Total utilisateurs</h3>
                <p><?= (int)($metrics['total_users'] ?? 0) ?></p>
            </div>
        </article>
        <article class="stat-card">
            <div class="stat-icon">🔥</div>
            <div class="stat-info">
                <h3>Utilisateurs actifs</h3>
                <p><?= (int)($metrics['active_users'] ?? 0) ?></p>
            </div>
        </article>
        <article class="stat-card">
            <div class="stat-icon">🤝</div>
            <div class="stat-info">
                <h3>Total affiliés</h3>
                <p><?= (int)($metrics['total_affiliates'] ?? 0) ?></p>
            </div>
        </article>
    </div>
    <footer class="stats-footer"></footer>
    <div class="stats-actions"></div>
    <div class="stats-summary"></div>
</section>

<section class="users-filters">
    <form method="GET" action="index.php" class="filters-form">
        <input type="hidden" name="action" value="admin-users" />
        <div class="filters-row">
            <div class="form-group">
                <label class="form-label">Recherche</label>
                <input type="text" name="search" value="<?= htmlspecialchars($filters['search'] ?? '') ?>" class="form-input" placeholder="Nom, prénom, email" />
            </div>
        </div>
        <div class="filters-actions">
            <button type="submit" class="btn-save">Filtrer</button>
        </div>
    </form>
    <div class="filters-summary"></div>
</section>

<section id="users-table-wrapper">
    <?php include 'app/views/users-table.phtml'; ?>
</section>

<script src="<?= BASE_URL ?>/app/public/js/adminUsers.js" defer></script>


