<!-- ========================================
DÉTAIL COMMANDE - ADMIN
======================================== -->

<!-- En-tête de la commande -->
<section class="order-header">
    <div class="order-info">
        <h2>
            <i class="fas fa-shopping-bag"></i>
            Commande #<?= (int)$order['id'] ?>
        </h2>
        <div class="order-meta">
            <span class="order-date">
                <i class="fas fa-calendar"></i>
                <?= date('d/m/Y à H:i', strtotime($order['order_date'])) ?>
            </span>
            <span class="order-status status-<?= strtolower(str_replace(' ', '-', $order['status'])) ?>">
                <?= htmlspecialchars($order['status']) ?>
            </span>
        </div>
    </div>
    <div class="order-actions">
        <a href="index.php?action=admin-orders-ticket&id=<?= (int)$order['id'] ?>" class="btn-print" target="_blank">
            <i class="fas fa-print"></i>
            Imprimer Ticket
        </a>
        <a href="index.php?action=admin-orders" class="btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
    </div>
</section>

<!-- Informations client -->
<section class="order-details-grid">
    <article class="admin-card">
        <header class="card-title">
            <h3><i class="fas fa-user"></i> Informations client</h3>
        </header>
        <div class="card-body">
            <div class="detail-row">
                <span class="detail-label">Nom:</span>
                <span class="detail-value"><?= htmlspecialchars($order['first_name'] . ' ' . $order['last_name']) ?></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Email:</span>
                <span class="detail-value"><?= htmlspecialchars($order['email'] ?? 'N/A') ?></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Téléphone:</span>
                <span class="detail-value"><?= htmlspecialchars($order['phone'] ?? 'N/A') ?></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Adresse:</span>
                <span class="detail-value"><?= htmlspecialchars($order['delivery_address'] ?? 'N/A') ?></span>
            </div>
            <div class="detail-row">
                <span class="detail-label">Ville:</span>
                <span class="detail-value"><?= htmlspecialchars($order['city_name'] ?? 'N/A') ?></span>
            </div>
            <?php if (!empty($order['neighborhood_name'])): ?>
            <div class="detail-row">
                <span class="detail-label">Quartier:</span>
                <span class="detail-value"><?= htmlspecialchars($order['neighborhood_name']) ?></span>
            </div>
            <?php endif; ?>
            <?php if (!empty($order['zone_code'])): ?>
            <div class="detail-row">
                <span class="detail-label">Zone:</span>
                <span class="detail-value"><?= htmlspecialchars($order['zone_code']) ?></span>
            </div>
            <?php endif; ?>
        </div>
    </article>
</section>

<!-- Détails de la commande -->
<section class="order-content">
    <div class="order-items-header">
        <h3>
            <i class="fas fa-receipt"></i>
            Détails de la commande
        </h3>
        <div class="order-meta-info">
            <span class="order-id">Commande #<?= (int)$order['id'] ?></span>
            <span class="order-date"><?= date('d/m/Y à H:i', strtotime($order['order_date'])) ?></span>
            <span class="status-badge status-<?= strtolower(str_replace(' ', '-', $order['status'])) ?>">
                <?= htmlspecialchars($order['status']) ?>
            </span>
        </div>
    </div>

    <?php if (!empty($orderDetails)): ?>
    <div class="order-items-table-container">
        <table class="admin-table">
            <thead>
                <tr>
                    <th>Produit</th>
                    <th>Quantité</th>
                    <th>Prix unitaire</th>
                    <th>Sous-total</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($orderDetails as $item): ?>
                <tr>
                    <td>
                        <div class="item-info">
                            <strong><?= htmlspecialchars($item['item_label']) ?></strong>
                            <?php if ($item['order_custom_salads_id'] && isset($customSalads[$item['order_custom_salads_id']])): ?>
                            <div class="custom-salad-details">
                                <small>
                                    <i class="fas fa-utensils"></i>
                                    Salade personnalisée
                                </small>
                                <?php if (!empty($customSalads[$item['order_custom_salads_id']]['note'])): ?>
                                <br><small class="custom-note">
                                    Note: <?= htmlspecialchars($customSalads[$item['order_custom_salads_id']]['note']) ?>
                                </small>
                                <?php endif; ?>
                            </div>
                            <?php endif; ?>
                        </div>
                    </td>
                    <td class="text-center"><?= (int)$item['quantity'] ?></td>
                    <td class="text-right"><?= number_format($item['unit_price'], 0, ',', ' ') ?> FCFA</td>
                    <td class="text-right">
                        <strong><?= number_format($item['subtotal'], 0, ',', ' ') ?> FCFA</strong>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
            <tfoot>
                <?php
                // Calculer le sous-total des produits (somme de tous les items)
                $productsSubtotal = 0;
                foreach ($orderDetails as $item) {
                    $productsSubtotal += $item['subtotal'];
                }
                ?>
                <tr class="subtotal-row">
                    <td colspan="3"><strong>Sous-total produits:</strong></td>
                    <td class="text-right">
                        <strong><?= number_format($productsSubtotal, 0, ',', ' ') ?> FCFA</strong>
                    </td>
                </tr>
                <?php if ($order['loyalty_points_used'] > 0): ?>
                <tr class="loyalty-row">
                    <td colspan="3"><strong>Points utilisés:</strong></td>
                    <td class="text-right">
                        <strong>-<?= number_format($order['loyalty_points_used'], 0, ',', ' ') ?> FCFA</strong>
                    </td>
                </tr>
                <?php endif; ?>
                <?php if ($order['delivery_fee'] > 0): ?>
                <tr class="delivery-row">
                    <td colspan="3"><strong>Frais de livraison:</strong></td>
                    <td class="text-right">
                        <strong><?= number_format($order['delivery_fee'], 0, ',', ' ') ?> FCFA</strong>
                    </td>
                </tr>
                <?php endif; ?>
                <tr class="total-row">
                    <td colspan="3"><strong>Total:</strong></td>
                    <td class="text-right">
                        <strong class="total-amount"><?= number_format($productsSubtotal - $order['loyalty_points_used'] + $order['delivery_fee'], 0, ',', ' ') ?> FCFA</strong>
                    </td>
                </tr>
            </tfoot>
        </table>
    </div>
    <?php else: ?>
    <div class="empty-state">
        <div class="empty-icon">
            <i class="fas fa-shopping-cart"></i>
        </div>
        <h3>Aucun article trouvé</h3>
        <p>Cette commande ne contient aucun article.</p>
    </div>
    <?php endif; ?>
</section>

<!-- Actions sur la commande -->
<section class="order-actions-section">
    <div class="actions-header">
        <h3>
            <i class="fas fa-cogs"></i>
            Actions
        </h3>
    </div>
    
    <div class="actions-grid">
        <!-- Changement de statut -->
        <form method="POST" action="index.php?action=admin-orders-update-status" class="action-form">
            <input type="hidden" name="csrf_token" value="<?= $csrfToken ?>">
            <input type="hidden" name="order_id" value="<?= (int)$order['id'] ?>">
            
            <div class="form-group">
                <label class="form-label">
                    <i class="fas fa-edit"></i>
                    Changer le statut
                </label>
                <div class="status-tabs">
                    <button type="button" class="status-tab <?= $order['status'] === 'En attente' ? 'active' : '' ?>" data-status="En attente">En attente</button>
                    <button type="button" class="status-tab <?= $order['status'] === 'En preparation' ? 'active' : '' ?>" data-status="En preparation">En préparation</button>
                    <button type="button" class="status-tab <?= $order['status'] === 'Livraison en cours' ? 'active' : '' ?>" data-status="Livraison en cours">Livraison</button>
                    <button type="button" class="status-tab <?= $order['status'] === 'Livrée' ? 'active' : '' ?>" data-status="Livrée">Livrée</button>
                    <button type="button" class="status-tab <?= $order['status'] === 'annulée' ? 'active' : '' ?>" data-status="annulée">Annulée</button>
                </div>
                <!-- Champ caché pour le statut sélectionné -->
                <input type="hidden" id="status" name="status" value="<?= htmlspecialchars($order['status']) ?>" required>
            </div>
            
            <button type="submit" class="btn-save">
                <i class="fas fa-save"></i>
                Mettre à jour
            </button>
        </form>

        <!-- Annulation -->
        <?php if ($order['status'] !== 'annulée' && $order['status'] !== 'Livrée'): ?>
        <form method="POST" action="index.php?action=admin-orders-cancel" class="action-form">
            <input type="hidden" name="csrf_token" value="<?= $csrfToken ?>">
            <input type="hidden" name="order_id" value="<?= (int)$order['id'] ?>">
            
            <div class="form-group">
                <label class="form-label">
                    <i class="fas fa-times-circle"></i>
                    Annuler la commande
                </label>
                <p class="form-help">Cette action ne peut pas être annulée.</p>
            </div>
            
            <button type="submit" class="btn-delete" 
                    onclick="return confirm('Êtes-vous sûr de vouloir annuler cette commande ?')">
                <i class="fas fa-ban"></i>
                Annuler
            </button>
        </form>
        <?php endif; ?>
    </div>
</section>

<script>
// Gestion des onglets de statut avec soumission automatique
document.addEventListener("DOMContentLoaded", function () {
    const statusTabs = document.querySelectorAll(".status-tab");
    const statusInput = document.getElementById("status");
    const form = document.querySelector(".action-form");

    statusTabs.forEach(tab => {
        tab.addEventListener("click", function () {
            // Retirer la classe active de tous les onglets
            statusTabs.forEach(t => t.classList.remove("active"));
            // Ajouter la classe active à l'onglet cliqué
            this.classList.add("active");
            
            // Mettre à jour la valeur du champ caché
            statusInput.value = this.dataset.status;
            
            // Soumettre automatiquement le formulaire
            form.submit();
        });
    });
});
</script>
