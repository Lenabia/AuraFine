<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<?= htmlspecialchars($csrfToken ?? '') ?>">
    <title>Administration - AuraFine</title>

    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <!-- RemixIcon -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.6.0/remixicon.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <!-- Google font  -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=PT+Sans:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="app/public/css/main.css" rel="stylesheet" />
</head>
<body>

<main class="admin-container">
    <!-- Overlay pour mobile -->
    <aside class="admin-overlay" id="admin-overlay"></aside>
    
    <!-- Sidebar -->
    <nav class="admin-sidebar" id="admin-sidebar">
        <header class="sidebar-header">
            <h1>
                <i class="fas fa-crown"></i>
                AuraFine Admin
            </h1>
            <address class="admin-role">Administrateur</address>
            <button class="sidebar-close" id="sidebar-close" aria-label="Fermer le menu">
                <i class="fas fa-times"></i>
            </button>
        </header>
        
        <section class="sidebar-nav">
            <a href="index.php?action=admin-home" class="nav-item <?= ($currentPage === 'admin-home') ? 'active' : '' ?>">
                <i class="fas fa-tachometer-alt"></i>
                Dashboard
            </a>
            <a href="index.php?action=admin-orders" class="nav-item <?= ($currentPage === 'admin-orders') ? 'active' : '' ?>">
                <i class="fas fa-shopping-bag"></i>
                Commandes
            </a>
            <a href="index.php?action=admin-catalog" class="nav-item <?= ($currentPage === 'admin-catalog') ? 'active' : '' ?>">
                <i class="fas fa-book"></i>
                Catalogue
            </a>
            <a href="index.php?action=admin-users" class="nav-item <?= ($currentPage === 'admin-users') ? 'active' : '' ?>">
                <i class="fas fa-users"></i>
                Utilisateurs
            </a>
            <a href="index.php?action=admin-delivery" class="nav-item <?= ($currentPage === 'admin-delivery') ? 'active' : '' ?>">
                <i class="fas fa-truck"></i>
                Livraison
            </a>
            <a href="index.php?action=admin-settings" class="nav-item <?= ($currentPage === 'admin-settings') ? 'active' : '' ?>">
                <i class="fas fa-cog"></i>
                Paramètres
            </a>
        </section>
        
        <footer class="sidebar-footer">
            <button class="logout-btn" id="logout-btn" onclick="showLogoutModal()">
                <i class="fas fa-sign-out-alt"></i>
                Déconnexion
            </button>
        </footer>
    </nav>
    
    <!-- Main Content -->
    <main class="admin-main">
        <!-- Header -->
        <header class="admin-header">
            <section class="header-left">
                <button class="menu-toggle" id="menu-toggle" aria-label="Ouvrir le menu">
                    <i class="fas fa-bars"></i>
                </button>
                <h2 class="page-title"><?= $pageTitle ?? 'Dashboard' ?></h2>
            </section>
            <section class="header-right">
                <address class="admin-user">
                    <i class="fas fa-user-circle"></i>
                    <span><?= $_SESSION['user']['first_name'] ?? 'Admin' ?></span>
                </address>
            </section>
        </header>
        
        <!-- Fil d'Ariane -->
        <?php if (isset($breadcrumbs) && !empty($breadcrumbs)): ?>
        <nav class="breadcrumb" aria-label="Fil d'Ariane">
            <ol class="breadcrumb-list">
                <?php foreach ($breadcrumbs as $index => $breadcrumb): ?>
                    <?php if ($index === count($breadcrumbs) - 1): ?>
                        <!-- Dernier élément (page actuelle) -->
                        <li class="breadcrumb-item current" aria-current="page">
                            <i class="<?= $breadcrumb['icon'] ?? 'fas fa-circle' ?>"></i>
                            <?= htmlspecialchars($breadcrumb['title']) ?>
                        </li>
                    <?php else: ?>
                        <!-- Éléments précédents -->
                        <li class="breadcrumb-item">
                            <a href="<?= htmlspecialchars($breadcrumb['url']) ?>" class="breadcrumb-link">
                                <i class="<?= $breadcrumb['icon'] ?? 'fas fa-circle' ?>"></i>
                                <?= htmlspecialchars($breadcrumb['title']) ?>
                            </a>
                        </li>
                        <li class="breadcrumb-separator">
                            <i class="fas fa-chevron-right"></i>
                        </li>
                    <?php endif; ?>
                <?php endforeach; ?>
            </ol>
        </nav>
        <?php endif; ?>
        
        <!-- Content -->
        <section class="admin-content">
            <?php include $template ?>
        </section>
    </main>
</main>

<!-- Modales -->
<div id="deleteModal" class="modal-overlay">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-icon">🗑️</div>
      <h3 class="modal-title">Supprimer</h3>
    </div>
    <div class="modal-body">
      <p class="modal-message"></p>
    </div>
    <div class="modal-actions">
      <button type="button" class="modal-btn modal-btn-cancel" onclick="closeModal()">Annuler</button>
      <button type="button" class="modal-btn modal-btn-confirm">Supprimer</button>
    </div>
  </div>
</div>

<div id="logoutModal" class="modal-overlay">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-icon">🚪</div>
      <h3 class="modal-title">Déconnexion</h3>
    </div>
    <div class="modal-body">
      <p class="modal-message">Êtes-vous sûr de vouloir vous déconnecter ?</p>
    </div>
    <div class="modal-actions">
      <button type="button" class="modal-btn modal-btn-cancel" onclick="closeModal()">Annuler</button>
      <a href="index.php?action=logout" class="modal-btn modal-btn-logout">Déconnexion</a>
    </div>
  </div>
</div>

<div id="cancelOrderModal" class="modal-overlay">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-icon">⚠️</div>
      <h3 class="modal-title">Annuler la commande</h3>
    </div>
    <div class="modal-body">
      <p class="modal-message">Êtes-vous sûr de vouloir annuler cette commande ? Cette action ne peut pas être annulée.</p>
    </div>
    <div class="modal-actions">
      <button type="button" class="modal-btn modal-btn-cancel" onclick="closeModal()">Annuler</button>
      <button type="button" class="modal-btn modal-btn-confirm">Confirmer l'annulation</button>
    </div>
  </div>
</div>

<div id="reactivateOrderModal" class="modal-overlay">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-icon">🔄</div>
      <h3 class="modal-title">Réactiver la commande</h3>
    </div>
    <div class="modal-body">
      <p class="modal-message">Êtes-vous sûr de vouloir réactiver cette commande ? Elle retrouvera son statut précédent.</p>
    </div>
    <div class="modal-actions">
      <button type="button" class="modal-btn modal-btn-cancel" onclick="closeModal()">Annuler</button>
      <button type="button" class="modal-btn modal-btn-confirm">Confirmer la réactivation</button>
    </div>
  </div>
</div>

<!-- Modale de réactivation de quartier -->
<div id="reactivateNeighborhoodModal" class="modal-overlay">
  <div class="modal-container">
    <div class="modal-header">
      <div class="modal-icon">🏘️</div>
      <h3 class="modal-title">Réactiver le quartier</h3>
    </div>
    <div class="modal-body">
      <p id="reactivateNeighborhoodMessage">Êtes-vous sûr de vouloir réactiver ce quartier ?</p>
    </div>
    <div class="modal-actions">
      <button type="button" class="modal-btn modal-btn-cancel" onclick="closeReactivateNeighborhoodModal()">Annuler</button>
      <button type="button" class="modal-btn modal-btn-confirm" onclick="confirmReactivateNeighborhood()">Confirmer la réactivation</button>
    </div>
  </div>
</div>

<!-- Scripts -->
<script src="/app/public/js/modal.js" defer></script>
<script src="/app/public/js/admin.js" defer></script>

<!-- Scripts spécifiques aux pages -->
<?php if (isset($currentPage) && $currentPage === 'admin-delivery'): ?>
<script src="/app/public/js/admin-delivery.js" defer></script>
<?php endif; ?>

<?php if (isset($currentPage) && $currentPage === 'admin-orders'): ?>
<script src="/app/public/js/orders.js" defer></script>
<?php endif; ?>

</body>
</html>
