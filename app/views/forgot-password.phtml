<!-- Page de mot de passe oublié -->
<section class="auth-page">
  <div class="auth-wrapper">
    <!-- Image décorative -->
    <div class="auth-image">
      <img src="<?= BASE_URL ?>/app/public/images/login-illustration.svg" alt="Mot de passe oublié AuraFine" class="auth-illustration">
    </div>
    
    <!-- Formulaire de mot de passe oublié -->
    <div class="auth-container">
      <div class="auth-card">
        <header class="auth-header">
          <h1 class="auth-title">Mot de passe oublié</h1>
          <p class="auth-subtitle">Entrez votre adresse email pour recevoir un lien de réinitialisation</p>
        </header>

        <?php if (isset($_SESSION['success_message'])): ?>
          <div class="auth-success" role="alert">
            <?= htmlspecialchars($_SESSION['success_message']) ?>
          </div>
          <?php unset($_SESSION['success_message']); ?>
        <?php endif; ?>

        <?php if (isset($errors['general'])): ?>
          <div class="auth-error" role="alert">
            <?= htmlspecialchars($errors['general']) ?>
          </div>
        <?php endif; ?>

        <form action="index.php?action=forgot-password" method="post" class="auth-form" novalidate>
          <!-- CSRF côté serveur -->
          <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf_token ?? '') ?>">

          <!-- Email -->
          <div class="auth-field">
            <label for="auth-email" class="auth-label">Adresse email</label>
            <input 
              type="email" 
              id="auth-email" 
              name="email" 
              class="auth-input" 
              autocomplete="email" 
              placeholder="votre@email.com" 
              value="<?= htmlspecialchars($old['email'] ?? '') ?>"
              required
            >
            <div class="auth-error" aria-live="polite"><?= $errors['email'] ?? '' ?></div>
          </div>

          <button class="auth-submit" type="submit">
            <i class="fas fa-paper-plane"></i>
            Envoyer le lien de réinitialisation
          </button>

          <div class="auth-separator"><span>ou</span></div>

          <div class="auth-links">
            <a href="index.php?action=login" class="auth-link">
              <i class="fas fa-arrow-left"></i>
              Retour à la connexion
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>


