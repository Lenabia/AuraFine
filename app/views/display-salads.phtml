<!-- ========================================
PAGE GESTION DES SALADES - LISTE ET ACTIONS
======================================== -->

<!-- En-tête avec bouton d'ajout -->
<section class="salads-header">
    <header>
        <h2>Gestion des Salades</h2>
        <p>Créez et gérez vos salades fraîches</p>
    </header>
    <nav class="header-actions">
        <a href="index.php?action=admin-catalog" class="btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour au Catalogue
        </a>
        <a href="index.php?action=admin-salads-create" class="btn-add">
            <i class="fas fa-plus"></i>
            Ajouter une salade
        </a>
    </nav>
</section>

<!-- Messages de succès/erreur -->
<?php if (isset($_SESSION['success_message'])): ?>
    <div class="alert alert-success" role="alert">
        <i class="fas fa-check-circle"></i>
        <?= htmlspecialchars($_SESSION['success_message']) ?>
    </div>
    <?php unset($_SESSION['success_message']); ?>
<?php endif; ?>

<?php if (isset($_SESSION['error_message'])): ?>
    <div class="alert alert-error" role="alert">
        <i class="fas fa-exclamation-circle"></i>
        <?= htmlspecialchars($_SESSION['error_message']) ?>
    </div>
    <?php unset($_SESSION['error_message']); ?>
<?php endif; ?>

<!-- Statistiques -->
<section class="salads-stats">
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-leaf"></i>
        </div>
        <div class="stat-content">
            <h3><?= $totalSalads ?? 0 ?></h3>
            <p>Salades au total</p>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-check-circle"></i>
        </div>
        <div class="stat-content">
            <h3><?= count(array_filter($salads, function($salad) { return $salad['is_available'] == 1; })) ?></h3>
            <p>Disponibles</p>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-times-circle"></i>
        </div>
        <div class="stat-content">
            <h3><?= count(array_filter($salads, function($salad) { return $salad['is_available'] == 0; })) ?></h3>
            <p>Désactivées</p>
        </div>
    </div>
</section>

<!-- Liste des salades -->
<section class="salads-list">
    <?php if (!empty($salads)): ?>
        <div class="salads-grid">
            <?php foreach ($salads as $salad): ?>
                <article class="salad-card">
                    <header class="salad-header">
                        <?php if (!empty($salad['image'])): ?>
                            <figure class="salad-image">
                                <img src="<?= htmlspecialchars($salad['image']) ?>" 
                                     alt="<?= htmlspecialchars($salad['name']) ?>"
                                     loading="lazy">
                            </figure>
                        <?php else: ?>
                            <figure class="salad-image placeholder">
                                <i class="fas fa-leaf"></i>
                            </figure>
                        <?php endif; ?>
                        
                        <div class="salad-status">
                            <?php if ($salad['is_available']): ?>
                                <span class="status-badge available">
                                    <i class="fas fa-check"></i>
                                    Disponible
                                </span>
                            <?php else: ?>
                                <span class="status-badge unavailable">
                                    <i class="fas fa-times"></i>
                                    Indisponible
                                </span>
                            <?php endif; ?>
                        </div>
                    </header>
                    
                    <section class="salad-content">
                        <h3 class="salad-name"><?= htmlspecialchars($salad['name']) ?></h3>
                        
                        <?php if (!empty($salad['description'])): ?>
                            <p class="salad-description"><?= htmlspecialchars($salad['description']) ?></p>
                        <?php endif; ?>
                        
                        <div class="salad-details">
                            <div class="detail-item">
                                <i class="fas fa-tag"></i>
                                <span><?= number_format($salad['price'], 0, ',', ' ') ?> FCFA</span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-boxes"></i>
                                <span>Stock: <?= $salad['stock_quantity'] ?></span>
                            </div>
                        </div>
                    </section>
                    
                    <footer class="salad-actions">
                        <a href="index.php?action=admin-salads-edit&id=<?= $salad['id'] ?>" 
                           class="btn-edit" title="Modifier">
                            <i class="fas fa-edit"></i>
                            Modifier
                        </a>
                        
                        <form method="POST" action="index.php?action=admin-salads-delete&id=<?= $salad['id'] ?>" 
                              class="delete-form" 
                              onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette salade ?')">
                            <input type="hidden" name="csrf_token" value="<?= $csrfToken ?>">
                            <button type="submit" class="btn-delete" title="Supprimer">
                                <i class="fas fa-trash"></i>
                                Supprimer
                            </button>
                        </form>
                    </footer>
                </article>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <!-- Aucune salade -->
        <section class="empty-state">
            <figure class="empty-icon">
                <i class="fas fa-leaf"></i>
            </figure>
            <h3>Aucune salade créée</h3>
            <p>Commencez par créer votre première salade</p>
            <a href="index.php?action=admin-salads-create" class="btn-add">
                <i class="fas fa-plus"></i>
                Ajouter une salade
            </a>
        </section>
    <?php endif; ?>
</section>
