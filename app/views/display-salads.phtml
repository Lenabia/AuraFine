<!-- ========================================
PAGE GESTION DES SALADES - LISTE ET ACTIONS
======================================== -->

<!-- refactor: utilisation de la classe générique .item-header -->
<section class="item-header">
    <header>
        <h2>Gestion des Salades</h2>
        <p>Créez et gérez vos salades fraîches</p>
    </header>
    <nav class="header-actions">
        <!-- refactor: utilisation de la classe générique .item-btn-back -->
        <a href="index.php?action=admin-catalog" class="item-btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour au Catalogue
        </a>
        <!-- refactor: utilisation de la classe générique .item-btn-add -->
        <a href="index.php?action=admin-salads-create" class="item-btn-add">
            <i class="fas fa-plus"></i>
            Ajouter une salade
        </a>
    </nav>
</section>

<!-- Messages de succès/erreur -->
<?php if (isset($_SESSION['success_message'])): ?>
    <div class="alert alert-success" role="alert">
        <i class="fas fa-check-circle"></i>
        <?= htmlspecialchars($_SESSION['success_message']) ?>
    </div>
    <?php unset($_SESSION['success_message']); ?>
<?php endif; ?>

<?php if (isset($_SESSION['error_message'])): ?>
    <div class="alert alert-error" role="alert">
        <i class="fas fa-exclamation-circle"></i>
        <?= htmlspecialchars($_SESSION['error_message']) ?>
    </div>
    <?php unset($_SESSION['error_message']); ?>
<?php endif; ?>

<!-- refactor: utilisation de la classe générique .item-stats -->
<section class="item-stats">
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-leaf"></i>
        </div>
        <div class="stat-content">
            <h3><?= $totalSalads ?? 0 ?></h3>
            <p>Salades au total</p>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-check-circle"></i>
        </div>
        <div class="stat-content">
            <h3><?= count(array_filter($salads, function($salad) { return $salad['is_available'] == 1; })) ?></h3>
            <p>Disponibles</p>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-star"></i>
        </div>
        <div class="stat-content">
            <h3><?= count(array_filter($salads, function($salad) { return $salad['is_available'] == 2; })) ?></h3>
            <p>Victimes de succès</p>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon">
            <i class="fas fa-times-circle"></i>
        </div>
        <div class="stat-content">
            <h3><?= count(array_filter($salads, function($salad) { return $salad['is_available'] == 0; })) ?></h3>
            <p>Désactivées</p>
        </div>
    </div>
</section>

<!-- refactor: utilisation de la classe générique .item-list -->
<section class="item-list">
    <?php if (!empty($salads)): ?>
        <!-- refactor: utilisation de la classe générique .item-grid -->
        <div class="item-grid">
            <?php foreach ($salads as $salad): ?>
                <!-- refactor: utilisation de la classe générique .item-card -->
                <article class="item-card">
                    <header class="item-header">
                        <?php if (!empty($salad['image'])): ?>
                            <figure class="item-image">
                                <img src="<?= htmlspecialchars($salad['image']) ?>" 
                                     alt="<?= htmlspecialchars($salad['name']) ?>"
                                     loading="lazy">
                            </figure>
                        <?php else: ?>
                            <figure class="item-image placeholder">
                                <i class="fas fa-leaf"></i>
                            </figure>
                        <?php endif; ?>
                        
                        <div class="item-status">
                            <?php if ($salad['is_available'] == 1): ?>
                                <span class="status-badge available">
                                    <i class="fas fa-check"></i>
                                    Disponible
                                </span>
                            <?php elseif ($salad['is_available'] == 2): ?>
                                <span class="status-badge victim">
                                    <i class="fas fa-star"></i>
                                    Victime de son succès
                                </span>
                            <?php else: ?>
                                <span class="status-badge unavailable">
                                    <i class="fas fa-times"></i>
                                    Indisponible
                                </span>
                            <?php endif; ?>
                        </div>
                    </header>
                    
                    <section class="item-content">
                        <h3 class="item-name"><?= htmlspecialchars($salad['name']) ?></h3>
                        
                        <?php if (!empty($salad['description'])): ?>
                            <p class="item-description"><?= htmlspecialchars($salad['description']) ?></p>
                        <?php endif; ?>
                        
                        <div class="item-details">
                            <div class="detail-item">
                                <i class="fas fa-tag"></i>
                                <span><?= number_format($salad['price'], 0, ',', ' ') ?> FCFA</span>
                            </div>
                            <div class="detail-item">
                                <i class="fas fa-boxes"></i>
                                <span>Stock: <?= $salad['stock_quantity'] ?></span>
                            </div>
                        </div>
                    </section>
                    
                    <footer class="item-actions">
                        <!-- refactor: utilisation de la classe générique .item-btn-edit -->
                        <a href="index.php?action=admin-salads-edit&id=<?= $salad['id'] ?>" 
                           class="item-btn-edit" title="Modifier">
                            <i class="fas fa-edit"></i>
                            Modifier
                        </a>
                        
                        <form method="POST" action="index.php?action=admin-salads-delete&id=<?= $salad['id'] ?>" 
                              class="delete-form" 
                              id="deleteForm<?= $salad['id'] ?>">
                            <input type="hidden" name="csrf_token" value="<?= $csrfToken ?>">
                            <!-- refactor: utilisation de la classe générique .item-btn-delete -->
                            <button type="button" class="item-btn-delete" 
                                    title="Supprimer définitivement"
                                    onclick="showDeleteModal('<?= htmlspecialchars($salad['name']) ?>', 'cette salade', 'deleteForm<?= $salad['id'] ?>')">
                                <i class="fas fa-trash"></i>
                                Supprimer
                            </button>
                        </form>
                    </footer>
                </article>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <!-- Aucune salade -->
        <section class="empty-state">
            <figure class="empty-icon">
                <i class="fas fa-leaf"></i>
            </figure>
            <h3>Aucune salade créée</h3>
            <p>Commencez par créer votre première salade</p>
            <!-- refactor: utilisation de la classe générique .item-btn-add -->
            <a href="index.php?action=admin-salads-create" class="item-btn-add">
                <i class="fas fa-plus"></i>
                Ajouter une salade
            </a>
        </section>
    <?php endif; ?>
</section>
