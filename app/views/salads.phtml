<main class="salad-page">
    <section class="salad-hero">
        <header>
            <h1>Nos Salades Fraîches</h1>
            <p>Découvrez notre sélection de salades préparées avec des ingrédients frais et de qualité</p>
        </header>
    </section>

    <section class="salad-content">
        <?php if (isset($error)): ?>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-triangle"></i>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <?php if ($totalSalads > 0): ?>
            <div class="salad-stats">
                <p class="salad-count">
                    <i class="fas fa-leaf"></i>
                    <?= $totalSalads ?> salade<?= $totalSalads > 1 ? 's' : '' ?> disponible<?= $totalSalads > 1 ? 's' : '' ?>
                </p>
            </div>

            <div class="salad-grid">
                <?php foreach ($salads as $salad): ?>
                    <article class="salad-card <?= $salad['is_available'] == 2 ? 'victim-of-success' : '' ?>">
                        <figure class="salad-image">
                            <?php if (!empty($salad['image'])): ?>
                                <img src="<?= htmlspecialchars($salad['image']) ?>" 
                                     alt="<?= htmlspecialchars($salad['name']) ?>"
                                     loading="lazy">
                            <?php else: ?>
                                <div class="salad-placeholder">
                                    <i class="fas fa-leaf"></i>
                                </div>
                            <?php endif; ?>
                            
                            <?php if ($salad['is_available'] == 2): ?>
                                <div class="victim-overlay">
                                    <div class="victim-message">
                                        <i class="fas fa-star"></i>
                                        <span>Victime de son succès</span>
                                    </div>
                                </div>
                            <?php endif; ?>
                        </figure>
                        
                        <div class="salad-info">
                            <h3 class="salad-name"><?= htmlspecialchars($salad['name']) ?></h3>
                            
                            <?php if (!empty($salad['description'])): ?>
                                <p class="salad-description"><?= htmlspecialchars($salad['description']) ?></p>
                            <?php endif; ?>
                            
                            <div class="salad-details">
                                <div class="salad-price">
                                    <span class="price-amount"><?= number_format($salad['price'], 0, ',', ' ') ?> FCFA</span>
                                </div>
                                
                                <div class="salad-stock">
                                    <?php if ($salad['is_available'] == 2): ?>
                                        <span class="stock-victim">
                                            <i class="fas fa-star"></i>
                                            Victime de son succès
                                        </span>
                                    <?php elseif ($salad['stock_quantity'] > 0): ?>
                                        <span class="stock-available">
                                            <i class="fas fa-check-circle"></i>
                                            En stock
                                        </span>
                                    <?php else: ?>
                                        <span class="stock-unavailable">
                                            <i class="fas fa-times-circle"></i>
                                            Rupture de stock
                                        </span>
                                    <?php endif; ?>
                                </div>
                            </div>
                            <div class="menu-actions">
                                <?php if ($salad['is_available'] == 2): ?>
                                    <button class="btn-add-to-cart disabled" disabled>
                                        <i class="fas fa-star"></i>
                                        Victime de son succès
                                    </button>
                                <?php else: ?>
                                    <button class="btn-add-to-cart" data-salad-id="<?= $salad['id'] ?>">
                                        <i class="fas fa-plus"></i>
                                        Ajouter au panier
                                    </button>
                                <?php endif; ?>
                            </div>
                        </div>
                    </article>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <div class="empty-state">
                <div class="empty-icon">
                    <i class="fas fa-leaf"></i>
                </div>
                <h3>Aucune salade disponible</h3>
                <p>Nous travaillons actuellement sur de nouvelles recettes. Revenez bientôt !</p>
            </div>
        <?php endif; ?>
    </section>
</main>