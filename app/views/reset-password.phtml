<!-- Page de réinitialisation de mot de passe -->
<section class="auth-page">
  <div class="auth-wrapper">
    <!-- Image décorative -->
    <div class="auth-image">
      <img src="<?= BASE_URL ?>/app/public/images/login-illustration.svg" alt="Réinitialisation mot de passe AuraFine" class="auth-illustration">
    </div>
    
    <!-- Formulaire de réinitialisation -->
    <div class="auth-container">
      <div class="auth-card">
        <header class="auth-header">
          <h1 class="auth-title">Nouveau mot de passe</h1>
          <p class="auth-subtitle">Choisissez un nouveau mot de passe sécurisé</p>
        </header>

        <?php if (isset($_SESSION['success_message'])): ?>
          <div class="auth-success" role="alert">
            <?= htmlspecialchars($_SESSION['success_message']) ?>
          </div>
          <?php unset($_SESSION['success_message']); ?>
        <?php endif; ?>

        <?php if (isset($errors['general'])): ?>
          <div class="auth-error" role="alert">
            <?= htmlspecialchars($errors['general']) ?>
          </div>
        <?php endif; ?>

        <form action="index.php?action=reset-password&token=<?= htmlspecialchars($token ?? '') ?>" method="post" class="auth-form" novalidate>
          <!-- CSRF côté serveur -->
          <input type="hidden" name="csrf_token" value="<?= htmlspecialchars($csrf_token ?? '') ?>">

          <!-- Nouveau mot de passe -->
          <div class="auth-field auth-password">
            <label for="auth-password" class="auth-label">Nouveau mot de passe</label>
            <div class="auth-password-wrap">
              <input 
                type="password" 
                id="auth-password" 
                name="password" 
                class="auth-input" 
                autocomplete="new-password" 
                placeholder="Votre nouveau mot de passe"
                required
              >
              <button type="button" class="auth-toggle-pass" aria-label="Afficher le mot de passe" aria-pressed="false">
                <i class="fas fa-eye" aria-hidden="true"></i>
              </button>
            </div>
            <div class="auth-error" aria-live="polite"><?= $errors['password'] ?? '' ?></div>
            <div class="auth-help">
              <small>Au moins 8 caractères avec majuscule, minuscule et chiffre</small>
            </div>
          </div>

          <!-- Confirmation du mot de passe -->
          <div class="auth-field auth-password">
            <label for="auth-password-confirm" class="auth-label">Confirmer le mot de passe</label>
            <div class="auth-password-wrap">
              <input 
                type="password" 
                id="auth-password-confirm" 
                name="password_confirm" 
                class="auth-input" 
                autocomplete="new-password" 
                placeholder="Confirmez votre nouveau mot de passe"
                required
              >
              <button type="button" class="auth-toggle-pass" aria-label="Afficher la confirmation" aria-pressed="false">
                <i class="fas fa-eye" aria-hidden="true"></i>
              </button>
            </div>
            <div class="auth-error" aria-live="polite"><?= $errors['password_confirm'] ?? '' ?></div>
          </div>

          <button class="auth-submit" type="submit">
            <i class="fas fa-key"></i>
            Modifier le mot de passe
          </button>

          <div class="auth-separator"><span>ou</span></div>

          <div class="auth-links">
            <a href="index.php?action=login" class="auth-link">
              <i class="fas fa-arrow-left"></i>
              Retour à la connexion
            </a>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>


