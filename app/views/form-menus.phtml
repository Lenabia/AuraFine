<!-- ========================================
FORMULAIRE CRÉATION/MODIFICATION MENU
======================================== -->

<!-- refactor: utilisation de la classe générique .item-header -->
<section class="item-header">
    <header>
        <h2><?= $isEdit ? 'Modifier le Menu' : 'Nouveau Menu' ?></h2>
        <p><?= $isEdit ? 'Modifiez les informations du menu' : 'Créez un nouveau menu' ?></p>
    </header>
    <nav class="header-actions">
        <!-- refactor: utilisation de la classe générique .item-btn-back -->
        <a href="index.php?action=admin-menus" class="item-btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
    </nav>
</section>

<!-- Messages d'erreur de validation -->
<?php if (isset($_SESSION['validation_errors'])): ?>
    <div class="alert alert-error" role="alert">
        <i class="fas fa-exclamation-triangle"></i>
        <h4>Erreurs de validation :</h4>
        <ul>
            <?php foreach ($_SESSION['validation_errors'] as $field => $error): ?>
                <li><strong><?= htmlspecialchars($field) ?>:</strong> <?= htmlspecialchars($error) ?></li>
            <?php endforeach; ?>
        </ul>
    </div>
    <?php unset($_SESSION['validation_errors']); ?>
<?php endif; ?>

<!-- Messages d'erreur générales -->
<?php if (isset($_SESSION['error_message'])): ?>
    <div class="alert alert-error" role="alert">
        <i class="fas fa-exclamation-triangle"></i>
        <?= htmlspecialchars($_SESSION['error_message']) ?>
    </div>
    <?php unset($_SESSION['error_message']); ?>
<?php endif; ?>

<!-- Formulaire -->
<section class="form-container">
    <!-- refactor: utilisation de la classe générique .item-form -->
    <form method="POST" action="<?= $isEdit ? 'index.php?action=admin-menus-update&id=' . $menu['id'] : 'index.php?action=admin-menus-store' ?>" 
          class="item-form" enctype="multipart/form-data" novalidate>
        
        <!-- Token CSRF pour la sécurité -->
        <input type="hidden" name="csrf_token" value="<?= $csrfToken ?>">
        
        <!-- Nom du menu -->
        <div class="form-group">
            <label for="name" class="form-label required">
                <i class="fas fa-utensils"></i>
                Nom du menu
            </label>
            <input type="text" 
                   id="name" 
                   name="name" 
                   class="form-input" 
                   value="<?= htmlspecialchars($menu['name'] ?? ($_SESSION['old_input']['name'] ?? '')) ?>"
                   placeholder="Ex: Menu Petit Déjeuner Complet"
                   required
                   maxlength="100">
            <small class="form-help">Le nom du menu (obligatoire, max 100 caractères)</small>
        </div>
        
        <!-- Catégorie -->
        <div class="form-group">
            <label for="categories_id" class="form-label required">
                <i class="fas fa-tag"></i>
                Catégorie
            </label>
            <select id="categories_id" 
                    name="categories_id" 
                    class="form-select" 
                    required>
                <option value="">Sélectionnez une catégorie</option>
                <?php foreach ($categories as $category): ?>
                    <?php 
                    $selectedCategoryId = $menu['categories_id'] ?? ($_SESSION['old_input']['categories_id'] ?? '');
                    $isSelected = ($selectedCategoryId == $category['id']) ? 'selected' : '';
                    ?>
                    <option value="<?= $category['id'] ?>" <?= $isSelected ?>>
                        <?= htmlspecialchars($category['name']) ?>
                    </option>
                <?php endforeach; ?>
            </select>
            <small class="form-help">Sélectionnez la catégorie du menu (obligatoire)</small>
        </div>
        
        <!-- Description -->
        <div class="form-group">
            <label for="description" class="form-label">
                <i class="fas fa-align-left"></i>
                Description
            </label>
            <textarea id="description" 
                      name="description" 
                      class="form-textarea" 
                      rows="4"
                      placeholder="Décrivez le contenu du menu et ses ingrédients..."
                      maxlength="1000"><?= htmlspecialchars($menu['description'] ?? ($_SESSION['old_input']['description'] ?? '')) ?></textarea>
            <small class="form-help">Description détaillée (optionnel, max 1000 caractères)</small>
        </div>
        
        <!-- Prix -->
        <div class="form-group">
            <label for="price" class="form-label required">
                <i class="fas fa-tag"></i>
                Prix (FCFA)
            </label>
            <input type="number" 
                   id="price" 
                   name="price" 
                   class="form-input" 
                   value="<?= htmlspecialchars($menu['price'] ?? ($_SESSION['old_input']['price'] ?? '')) ?>"
                   placeholder="5000"
                   min="0"
                   step="0.01"
                   required>
            <small class="form-help">Prix en francs CFA (obligatoire)</small>
        </div>
        
        <!-- Image -->
        <div class="form-group">
            <label for="image" class="form-label">
                <i class="fas fa-image"></i>
                Image du menu
            </label>
            
            <!-- Aperçu de l'image actuelle -->
            <?php if (!empty($menu['image'])): ?>
                <div class="current-image-preview">
                    <img src="<?= htmlspecialchars($menu['image']) ?>" 
                         alt="Image actuelle" 
                         style="max-width: 200px; max-height: 200px; border-radius: 0.5rem; margin-bottom: 1rem;">
                    <p class="current-image-text">Image actuelle</p>
                </div>
            <?php endif; ?>
            
            <!-- Input file pour l'upload -->
            <input type="file" 
                   id="image" 
                   name="image" 
                   class="form-input" 
                   accept="image/jpeg,image/jpg,image/png,image/webp">
            
            <small class="form-help">Sélectionnez une image (JPG, PNG, WEBP - max 2MB) ou laissez vide pour conserver l'image actuelle</small>
        </div>
        
        <!-- Disponibilité -->
        <div class="form-group">
            <label class="form-label">
                <i class="fas fa-toggle-on"></i>
                Statut de disponibilité
            </label>
            <div class="form-radio-group">
                <label class="radio-label">
                    <input type="radio" 
                           name="is_available" 
                           value="1" 
                           <?= (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) == '1' || (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) === 1)) ? 'checked' : '' ?>>
                    <span class="radio-custom"></span>
                    <span class="radio-text">Disponible à la vente</span>
                </label>
                
                <label class="radio-label">
                    <input type="radio" 
                           name="is_available" 
                           value="2" 
                           <?= (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) == '2' || (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) === 2)) ? 'checked' : '' ?>>
                    <span class="radio-custom"></span>
                    <span class="radio-text">Victime de son succès</span>
                </label>
                
                <label class="radio-label">
                    <input type="radio" 
                           name="is_available" 
                           value="0" 
                           <?= (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) == '0' || (($menu['is_available'] ?? ($_SESSION['old_input']['is_available'] ?? '1')) === 0)) ? 'checked' : '' ?>>
                    <span class="radio-custom"></span>
                    <span class="radio-text">Indisponible (caché)</span>
                </label>
            </div>
            <small class="form-help">Choisissez le statut d'affichage du menu</small>
        </div>
        
        <!-- refactor: utilisation des classes génériques pour les boutons -->
        <footer class="form-actions">
            <button type="submit" class="item-btn-save">
                <i class="fas fa-save"></i>
                <?= $isEdit ? 'Modifier le menu' : 'Créer le menu' ?>
            </button>
            
            <a href="index.php?action=admin-menus" class="item-btn-cancel">
                <i class="fas fa-times"></i>
                Annuler
            </a>
        </footer>
    </form>
</section>

<?php
// Nettoyer les données temporaires après affichage
unset($_SESSION['old_input']);
?>
